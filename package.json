{
  "name": "agentgrid",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/orchestrator",
    "packages/dashboard"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev -w orchestrator & npm run dev -w dashboard"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
